"use client";
import Link from "next/link";
import { Check, ChevronRight, HeartPulse, ShieldCheck } from "lucide-react";
import { useState } from "react";

const steps=[{title:"서울온병원에서 퇴원 후 회복을 함께 살펴드립니다.",body:"복잡한 설문 없이 오늘의 상태만 간단히 알려주세요."},{title:"간단히 본인을 확인할게요.",body:"병원에 등록한 휴대전화 끝 4자만 입력합니다."},{title:"생활 변화를 함께 살펴봐요.",body:"걸음과 활동 정보는 회복 흐름을 알아보는 데만 사용합니다."}];
export default function Onboarding(){const [step,setStep]=useState(0);return <main className="min-h-[100dvh] bg-[#EFF5F2] p-5 text-[#17251F]"><div className="mx-auto flex min-h-[calc(100dvh-40px)] max-w-[520px] flex-col rounded-[30px] bg-white p-6 shadow-xl sm:p-8"><div className="flex items-center gap-2 font-black text-[#1D6B5B]"><HeartPulse/>오늘안부 Care</div><div className="mt-6 h-2 overflow-hidden rounded-full bg-[#E4ECE8]"><div className="h-full bg-[#1D6B5B] transition-all" style={{width:`${((step+1)/steps.length)*100}%`}}/></div><section className="flex flex-1 flex-col justify-center py-8"><span className="mx-auto grid size-20 place-items-center rounded-full bg-[#E7F1ED] text-[#1D6B5B]">{step===2?<ShieldCheck size={38}/>:<Check size={38}/>}</span><p className="mt-8 text-center text-sm font-black text-[#1D6B5B]">안내 {step+1} / {steps.length}</p><h1 className="mt-3 text-center text-3xl font-black leading-[1.3]">{steps[step].title}</h1><p className="mt-5 text-center text-lg font-semibold leading-8 text-[#62716B]">{steps[step].body}</p>{step===1?<input inputMode="numeric" maxLength={4} aria-label="휴대전화 끝 4자리" placeholder="휴대전화 끝 4자리" className="mt-7 min-h-16 rounded-2xl border-2 border-[#BFD0C8] px-5 text-center text-xl font-black"/>:null}</section>{step<2?<button onClick={()=>setStep(step+1)} className="flex min-h-[68px] w-full items-center justify-center gap-2 rounded-2xl bg-[#1D6B5B] text-xl font-black text-white">다음 <ChevronRight/></button>:<Link href="/care/patient" className="flex min-h-[68px] w-full items-center justify-center rounded-2xl bg-[#1D6B5B] text-xl font-black text-white">오늘 회복 확인하기</Link>}<p className="mt-4 text-center text-sm font-semibold text-[#74817C]">급한 증상은 병원이나 119에 연락하세요.</p></div></main>}

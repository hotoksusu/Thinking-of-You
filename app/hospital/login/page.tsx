"use client";import Link from "next/link";import {useState} from "react";import {loginHospital} from "@/lib/demo-auth";
export default function Page(){const [email,setEmail]=useState("nurse@seoulon.demo"),[password,setPassword]=useState("demo1234"),[error,setError]=useState("");function login(){if(loginHospital(email,password))location.href="/hospital/dashboard";else setError("Demo 계정 정보를 확인해 주세요.")}return <main className="grid min-h-screen place-items-center bg-[#F1F0E9] p-5"><section className="w-full max-w-md rounded-3xl bg-white p-7"><p className="font-black text-[#315E50]">오늘안부 Care · Demo</p><h1 className="mt-2 text-3xl font-black">병원 관계자 로그인</h1><p className="mt-2 text-[#68766F]">개인별 계정으로 병원 데이터에 접근합니다.</p><label className="mt-6 block font-bold">이메일<input value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 min-h-14 w-full rounded-xl border-2 p-3"/></label><label className="mt-4 block font-bold">비밀번호<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 min-h-14 w-full rounded-xl border-2 p-3"/></label>{error?<p className="mt-3 font-bold text-red-700">{error}</p>:null}<button onClick={login} className="primary">로그인</button><div className="mt-5 rounded-xl bg-amber-50 p-4 text-sm"><strong>Demo Authentication</strong><p className="mt-1">비밀번호는 브라우저에 저장되지 않습니다.</p><p className="mt-2">병원 B: nurse@haeon.demo / demo1234</p></div><Link href="mailto:hello@oneulanbu.kr" className="mt-5 block text-center font-bold text-[#315E50]">오늘안부 도입 문의</Link></section></main>}

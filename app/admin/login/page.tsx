"use client";import {useState} from "react";import {loginOperator} from "@/lib/demo-auth";
export default function Page(){const [email,setEmail]=useState("operator@oneulanbu.demo"),[password,setPassword]=useState("admin1234"),[error,setError]=useState("");return <main className="grid min-h-screen place-items-center bg-slate-950 p-5"><section className="w-full max-w-md rounded-3xl bg-white p-7"><p className="font-black text-teal-700">내부 운영 전용 · Demo</p><h1 className="mt-2 text-3xl font-black">운영자 로그인</h1><label className="mt-6 block font-bold">이메일<input value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 min-h-14 w-full rounded-xl border-2 p-3"/></label><label className="mt-4 block font-bold">비밀번호<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 min-h-14 w-full rounded-xl border-2 p-3"/></label>{error?<p className="mt-3 font-bold text-red-700">{error}</p>:null}<button onClick={()=>{if(loginOperator(email,password))location.href="/admin";else setError("Demo 계정 정보를 확인해 주세요.")}} className="primary">로그인</button><p className="mt-4 text-sm text-slate-500">Production에서는 MFA 확인 단계를 추가합니다.</p></section></main>}

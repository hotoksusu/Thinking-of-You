import Image from "next/image";
import { CalendarDays, Check, ClipboardCheck, Footprints, MessageCircle, Moon, Phone, Search, Sparkles } from "lucide-react";

export type CareCompanionState="welcome"|"walking"|"resting"|"checking"|"calling"|"calendar"|"good"|"attention"|"waiting"|"completed"|"sleeping"|"celebrating";
const stateMeta={welcome:{Icon:MessageCircle,label:"반가워요"},walking:{Icon:Footprints,label:"평소처럼 회복 중"},resting:{Icon:Moon,label:"잠시 쉬어가요"},checking:{Icon:ClipboardCheck,label:"확인 중"},calling:{Icon:Phone,label:"병원에서 확인했어요"},calendar:{Icon:CalendarDays,label:"외래 일정이 있어요"},good:{Icon:Check,label:"잘 회복하고 있어요"},attention:{Icon:ClipboardCheck,label:"한번 확인해보면 좋겠어요"},waiting:{Icon:Search,label:"정보를 기다리고 있어요"},completed:{Icon:Check,label:"오늘 할 일을 마쳤어요"},sleeping:{Icon:Moon,label:"편안히 쉬어요"},celebrating:{Icon:Sparkles,label:"잘하셨어요"}} as const;
export function CareCompanion({state="welcome",message,compact=false}:{state?:CareCompanionState;message?:string;compact?:boolean}){const {Icon,label}=stateMeta[state];return <div className={`relative ${compact?"w-[110px]":"w-full max-w-[280px]"}`} role="img" aria-label={`오늘안부 Care Companion, ${label}`}><div className={`relative overflow-hidden rounded-[32px] bg-[#E8F1EA] ${compact?"aspect-square":"aspect-[4/3]"}`}><Image src="/brand/farm-mascot.png" alt="" fill sizes={compact?"110px":"280px"} className="object-cover"/><span className="absolute right-3 top-3 grid size-11 place-items-center rounded-2xl border border-white/80 bg-white/90 text-[#315E50] shadow-sm"><Icon size={22}/></span></div>{message?<p className="relative -mt-4 mx-3 rounded-2xl border border-[#D8E4DE] bg-white px-4 py-3 text-center font-black leading-6 text-[#28443A] shadow-sm">{message}</p>:null}</div>}

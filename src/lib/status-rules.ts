export type StatusInput={patientId:string;pain:number;painScore?:number;mobility:number;hasConcern:boolean;concernText:string;createdAt:string};
export type StatusResult={patientId:string;level:"stable"|"watch"|"needs_attention";reason:string;updatedAt:string};
// DEMO / PILOT workflow rule only. Not a medical risk score, diagnosis, or complication prediction.
export function evaluatePatientStatus(current:StatusInput,previous?:StatusInput):StatusResult{const worse=previous&&(current.pain>previous.pain||current.mobility>previous.mobility),painChange=typeof current.painScore==="number"?(typeof previous?.painScore==="number"?`통증 ${previous.painScore} → ${current.painScore}`:`통증 ${current.painScore} / 10`):"";if(current.pain===3||current.mobility===3||(current.pain>=2&&current.mobility>=2)||current.hasConcern){const reasons=[painChange,current.mobility===3?"거의 움직이지 못함":current.mobility>=2?"움직임 많이 힘듦":"",current.hasConcern?current.concernText:""] .filter(Boolean);return{patientId:current.patientId,level:"needs_attention",reason:reasons.join(" · ")||"환자 응답 확인 필요",updatedAt:current.createdAt}}if(current.pain===2||current.mobility===2||worse)return{patientId:current.patientId,level:"watch",reason:[painChange,worse?"이전 응답보다 불편해짐":"",current.mobility===2?"움직임 많이 힘듦":""].filter(Boolean).join(" · ")||"환자 응답 변화 확인",updatedAt:current.createdAt};return{patientId:current.patientId,level:"stable",reason:"오늘 응답에서 별도 확인이 필요한 변화 없음",updatedAt:current.createdAt}}

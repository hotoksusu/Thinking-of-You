export type StatusInput={patientId:string;pain:number;mobility:number;hasConcern:boolean;concernText:string;createdAt:string};
export type StatusResult={patientId:string;level:"stable"|"watch"|"needs_attention";reason:string;updatedAt:string};
// DEMO / PILOT workflow rule only. Not a medical risk score, diagnosis, or complication prediction.
export function evaluatePatientStatus(current:StatusInput,previous?:StatusInput):StatusResult{const worse=previous&&(current.pain>previous.pain||current.mobility>previous.mobility);if(current.pain===3||current.mobility===3||(current.pain>=2&&current.mobility>=2)||current.hasConcern){const reasons=[current.pain===3?"통증 응답 확인 필요":"",current.mobility===3?"움직임 응답 확인 필요":"",current.pain>=2&&current.mobility>=2?"통증과 움직임이 함께 불편함":"",current.hasConcern?`추가 불편 응답${current.concernText?`: ${current.concernText}`:""}`:""] .filter(Boolean);return{patientId:current.patientId,level:"needs_attention",reason:reasons.join(" · "),updatedAt:current.createdAt}}if(current.pain===2||current.mobility===2||worse)return{patientId:current.patientId,level:"watch",reason:worse?"이전 응답보다 한 단계 불편해짐":current.pain===2?"통증을 많이 느낀다고 응답함":"움직임이 조금 힘들다고 응답함",updatedAt:current.createdAt};return{patientId:current.patientId,level:"stable",reason:"오늘 응답에서 별도 확인이 필요한 변화 없음",updatedAt:current.createdAt}}

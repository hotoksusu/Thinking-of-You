import { audit,getHospitalSession,getPatientSession } from "@/lib/demo-auth";
export type CareEvent = "patient_invited"|"patient_onboarding_started"|"patient_onboarding_completed"|"checkin_started"|"checkin_completed"|"patient_status_changed"|"hospital_patient_opened"|"followup_started"|"followup_completed";
export function trackCareEvent(name:CareEvent,properties:Record<string,unknown>={}){if(typeof window==="undefined")return;window.dispatchEvent(new CustomEvent("todayanbu:analytics",{detail:{name,...properties,at:new Date().toISOString()}}));const patientId=String(properties.patientId||"");const hs=getHospitalSession(),ps=getPatientSession();if(name==="checkin_completed"&&ps)audit({actorType:"patient",actorId:ps.patientId,hospitalId:ps.hospitalId,action:"checkin_completed",resourceType:"checkin",resourceId:patientId});if(name==="patient_status_changed"&&ps)audit({actorType:"patient",actorId:ps.patientId,hospitalId:ps.hospitalId,action:"patient_status_changed",resourceType:"patient_status",resourceId:patientId});if(name==="hospital_patient_opened"&&hs)audit({actorType:"hospital_user",actorId:hs.userId,hospitalId:hs.hospitalId,action:"patient_record_viewed",resourceType:"patient",resourceId:patientId});if(name==="followup_completed"&&hs)audit({actorType:"hospital_user",actorId:hs.userId,hospitalId:hs.hospitalId,action:"followup_completed",resourceType:"patient",resourceId:patientId})}
